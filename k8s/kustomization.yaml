apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - hpa.yaml
  - pdb.yaml

# Common labels
commonLabels:
  app: shopping-list-client
  component: frontend
  managed-by: kustomize

# Image configuration (override in overlays)
# images:
# - name: shopping-list-client
#   newName: registry.example.com/shopping-list-client
#   newTag: v1.0.0
